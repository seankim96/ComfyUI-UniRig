# Environment manager (handles isolated environment and CUDA packages)
comfy-env>=0.0.12

# Base dependencies (needed by main ComfyUI process)
huggingface_hub
trimesh
numpy
scipy
pillow
python-box
einops
omegaconf
addict
timm
tqdm
